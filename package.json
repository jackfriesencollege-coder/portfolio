{
  "name": "jack-friesen-portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "dev:live": "astro build && wrangler pages dev dist --compatibility-date=2026-08-18",
    "check": "astro check",
    "deploy": "node scripts/deploy.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260924.1",
    "wrangler": "^4.123.0"
  }
}
