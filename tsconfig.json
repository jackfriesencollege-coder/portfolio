{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "//": "functions/ has its own tsconfig — it runs on Cloudflare, not in a browser.",
  "exclude": ["dist", "functions"],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@config": ["site.config.ts"]
    }
  }
}
